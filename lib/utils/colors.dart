import 'package:flutter/material.dart';

const Color green = Colors.green;
const Color grey = Colors.grey;
const Color transparent = Colors.transparent;
const Color primary = Colors.white;
const Color secondary = Colors.lightBlue;
const Color accents = Color(0xFF247BA0);
//Sunny
const Color bgSunny = Colors.lightBlueAccent;// (Light and calming blue)
const Color bgSunnyWarm = Colors.amberAccent;// (Warm and inviting yellow)

//Cloudy:
const Color bgCloudy = Colors.grey;// (Neutral and subdued)
const Color bgCloudyCalm = Colors.blueGrey;// (Calming and slightly cool)

//Rainy:
 Color bgRainy = Colors.blueGrey.shade700;// (Dark and moody blue-grey)
 Color bgRainyCalm = Colors.tealAccent.shade700;// (Deep and calming teal)

// Snowy:
const Color bgSnowy = Colors.white; //(Classic white for snow)
 Color bgSnowyLight = Colors.lightBlue.shade200; //(Light and cool blue for a wintery feel)

 //Drizzle:
 Color bgDrizzle = Colors.blueGrey.shade200;// (Light and misty blue-grey)
 Color bgDrizzleLight = Colors.lightBlue.withOpacity(0.7);// (Translucent light blue)

//Fog:
 Color bgFog = Colors.grey.shade200; //(Light and hazy grey)
 Color bgFogLight = Colors.lightBlue.withOpacity(0.5);// (Translucent light blue)

//Windy:
 Color bgWindy = Colors.lightBlue.shade700; //(Deep and calming blue)
 Color bgWindyEnergetic = Colors.tealAccent; //(Energetic and dynamic teal)

//Thunderstorm:
 Color bgThunderstorm = Colors.grey.shade800; //(Dark and stormy grey)
 Color bgThunderstormElectric = Colors.purpleAccent;// (Dramatic and electric purple)

//Overcast:
 Color bgOvercast = Colors.grey; //(Neutral and subdued)
 Color bgOvercastDark = Colors.blueGrey.shade400;// (Darker and moodier blue-grey)
//Mist:
 Color bgMist = Colors.lightBlue.shade100; //(Very light and hazy blue)
 Color bgMistOpcty = Colors.white.withOpacity(0.8); //(Translucent white)